# TODO: Consider renaming _remote_tools directory to _remote_services or _json_rpc_services
# The name _remote_tools is misleading since this package (_exec_async) provides infrastructure
# rather than a tool. See exec2_plan.md "Future Cleanup" section.
